

## Navbar um pouquinho mais larga

Mudar apenas o `pb-2` para `pb-3` no `<nav>` do `FuturisticNavbar.tsx` (linha 13). Isso adiciona 4px extras de padding inferior — o mínimo perceptível.

**Arquivo:** `src/components/FuturisticNavbar.tsx`
- `pb-2` → `pb-3`

Uma única mudança de classe CSS, nada mais.

