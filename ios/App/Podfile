platform :ios, '13.0'

target 'App' do
  use_frameworks!

  # Adicionando o RevenueCat diretamente via CocoaPods
  pod 'RevenueCat', '5.59.2'
  
  # Se você usa outros plugins do Capacitor que precisam de Pods, 
  # eles seriam listados aqui, mas o RevenueCat é o nosso foco principal.
end
